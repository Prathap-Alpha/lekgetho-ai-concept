# BURS's own source questionnaire — internal reference, never publish
_source_paper.txt

.claude/

# Manus deliverable package — contains partner contact details + internal brief. NEVER publish.
manus-package/
*.zip
